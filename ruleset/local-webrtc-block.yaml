payload:
  - "AND,((NETWORK,UDP),(DST-PORT,3478))"
  - "AND,((NETWORK,UDP),(DST-PORT,5349))"
  - "AND,((NETWORK,UDP),(DST-PORT,19302))"
  - "AND,((NETWORK,UDP),(DST-PORT,19303))"
  - "AND,((NETWORK,UDP),(DST-PORT,19304))"
  - "AND,((NETWORK,UDP),(DST-PORT,19305))"
  - "AND,((NETWORK,UDP),(DST-PORT,19306))"
  - "AND,((NETWORK,UDP),(DST-PORT,19307))"
  - "AND,((NETWORK,UDP),(DST-PORT,19308))"
  - "AND,((NETWORK,UDP),(DST-PORT,19309))"
